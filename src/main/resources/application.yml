spring:
  data:
    mongodb:
      auto-index-creation: true
  mongodb:
    uri: mongodb://admin:password@localhost:27017/aiagent?authSource=admin

logging:
  level:
    org.springframework.data.mongodb:
      core.MongoTemplate: DEBUG